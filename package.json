{
  "name": "pdfoo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:py": "venv/bin/uvicorn app.main:app --reload --port 8456",
    "dev:all": "concurrently -n vite,py \"npm run dev\" \"npm run dev:py\"",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "concurrently": "^9",
    "vite": "^6"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-shell": "^2"
  }
}
